from org.transcrypt.stubs.browser var __pragma__ = require('__pragma__');
var org.transcrypt.autotester = require('org.transcrypt.autotester');

var arguments = require('arguments');
var attribs_by_name = require('attribs_by_name');
inport os;

globalrange =50;



        p1=0;
        p2=0;
        p3=0;
        p1= random.randint(0,globalrange);
        p2= random.randint(0,globalrange);
        p3= random.randint(0,globalrange);

for (i in 20) {
	os.Popen('gen.py + p1 + p2 + p3');
}
